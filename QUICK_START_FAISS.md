# 🚀 Быстрый старт с FAISS Vector Store

## Что изменилось?

✅ **ChromaDB заменен на FAISS** - стабильная, быстрая векторная база данных от Facebook
✅ **Нет проблем с зависанием** - работает стабильно на Windows
✅ **Быстрая индексация** - 18 документов за ~3 секунды
✅ **Компактное хранение** - индекс занимает всего 0.11 MB

## Установка

```bash
# 1. Установить зависимости
pip install -r requirements.txt

# FAISS будет установлен автоматически
```

## Использование

### 1. Запуск бота

```bash
python main.py
```

Ожидаемый вывод:
```
INFO - Initializing FAISS vector store...
INFO - Created new FAISS index with dimension 1536
INFO - Adding 18 documents to vector store...
INFO - Successfully added all 18 documents
INFO - RAG ready with 18 document chunks
INFO - Bot started
```

### 2. Тестирование

```bash
python test_faiss_integration.py
```

Проверяет:
- ✅ Инициализацию индекса
- ✅ Индексацию документов
- ✅ Поиск по векторам
- ✅ RAG запросы с контекстом
- ✅ Статистику хранилища

### 3. Добавление документов

Поместите файлы в `data/documents/`:
- `.pdf` - PDF документы
- `.txt` - текстовые файлы
- `.md` - Markdown файлы

Документы автоматически индексируются при запуске.

## Структура данных

```
data/
├── faiss_index/
│   ├── index.faiss      # FAISS индекс с векторами
│   └── metadata.pkl     # Метаданные документов
└── documents/           # Ваши документы
    ├── file1.pdf
    ├── file2.txt
    └── file3.md
```

## Основные команды бота

```
/start          - Начать работу
/mode rag       - Включить режим RAG
/mode text      - Обычный режим GPT
/reset          - Сбросить историю
```

## Примеры запросов

В режиме RAG бот отвечает на основе ваших документов:

```
Пользователь: Что такое Python?
Бот: [Ответ на основе документов в data/documents/]

Пользователь: Как управлять проектом?
Бот: [Ответ с контекстом из ваших документов]
```

## Настройка параметров

В `config.py`:

```python
# Размер чанков документов
RAG_CHUNK_SIZE = 1000      # Символов в чанке
RAG_CHUNK_OVERLAP = 200    # Перекрытие между чанками

# Количество документов для контекста
RAG_TOP_K = 3              # Топ-3 релевантных документа
```

## Производительность

| Операция | Время |
|----------|-------|
| Индексация 18 документов | ~3 секунды |
| Поиск по индексу | <1 миллисекунды |
| RAG запрос (с GPT) | ~6 секунд |

## Troubleshooting

### Ошибка импорта FAISS
```bash
pip install --upgrade faiss-cpu
```

### Индекс не создается
Проверьте права на запись:
```bash
mkdir -p data/faiss_index
```

### Низкая релевантность
1. Уменьшите `RAG_CHUNK_SIZE` для более точных чанков
2. Увеличьте `RAG_TOP_K` для большего контекста
3. Улучшите качество исходных документов

### Переиндексация
```python
from rag.index import get_vector_index

index = get_vector_index()
index.index_documents_directory(force_reindex=True)
```

## Дополнительная документация

- 📖 `MIGRATION_TO_FAISS.md` - детали миграции
- 📖 `VECTOR_STORE_GUIDE.md` - полное руководство
- 📖 `FAISS_IMPLEMENTATION_SUMMARY.md` - технический отчет

## Поддержка

При возникновении проблем:
1. Проверьте логи в `bot.log`
2. Запустите `test_faiss_integration.py`
3. Убедитесь, что все зависимости установлены

## Что дальше?

- 🔧 Настройте параметры под ваши нужды
- 📄 Добавьте свои документы в `data/documents/`
- 🤖 Запустите бота и протестируйте RAG
- 📊 Мониторьте производительность через логи

---

**Готово!** Ваш бот с FAISS vector store готов к работе! 🎉
