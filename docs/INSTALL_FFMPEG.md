# Установка ffmpeg для голосовых сообщений

## Проблема

```
Couldn't find ffmpeg or avconv
Error converting audio: [WinError 2] Не удается найти указанный файл
```

## Зачем нужен ffmpeg?

ffmpeg используется для:
- Конвертации аудио форматов (OGG → MP3)
- Обработки голосовых сообщений
- Работы с аудио файлами

Без ffmpeg голосовые сообщения не будут работать.

---

## Установка на Windows

### Способ 1: winget (рекомендуется)

```powershell
winget install ffmpeg
```

**После установки:**
1. Закройте PowerShell
2. Откройте новый PowerShell
3. Проверьте: `ffmpeg -version`
4. Перезапустите бота

### Способ 2: Chocolatey

```powershell
# Установить Chocolatey (если еще не установлен)
Set-ExecutionPolicy Bypass -Scope Process -Force
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))

# Установить ffmpeg
choco install ffmpeg
```

### Способ 3: Вручную

1. **Скачать ffmpeg:**
   - https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip
   - Или: https://github.com/BtbN/FFmpeg-Builds/releases

2. **Распаковать:**
   - Создать папку `C:\ffmpeg`
   - Распаковать туда содержимое

3. **Добавить в PATH:**
   - Открыть "Система" → "Дополнительные параметры системы"
   - "Переменные среды"
   - В "Path" добавить `C:\ffmpeg\bin`
   - Нажать OK

4. **Перезапустить терминал**

---

## Установка на Linux

### Ubuntu/Debian:
```bash
sudo apt update
sudo apt install ffmpeg
```

### Fedora:
```bash
sudo dnf install ffmpeg
```

### Arch Linux:
```bash
sudo pacman -S ffmpeg
```

---

## Установка на macOS

### Homebrew:
```bash
brew install ffmpeg
```

### MacPorts:
```bash
sudo port install ffmpeg
```

---

## Проверка установки

```bash
# Проверить версию
ffmpeg -version

# Проверить ffprobe
ffprobe -version
```

**Ожидаемый вывод:**
```
ffmpeg version 8.0.1
...
```

---

## После установки

1. **Перезапустить терминал/PowerShell**
2. **Проверить установку:**
   ```bash
   ffmpeg -version
   ```
3. **Перезапустить бота:**
   ```bash
   python main.py
   ```

---

## Troubleshooting

### ffmpeg не найден после установки

**Решение:**
1. Закройте ВСЕ окна PowerShell/CMD
2. Откройте новое окно
3. Проверьте: `ffmpeg -version`

### PATH не обновился

**Решение:**
1. Перезагрузите компьютер
2. Или вручную добавьте в PATH:
   - Найдите где установлен ffmpeg
   - Добавьте путь к `bin` в переменные среды

### Ошибка "access denied"

**Решение:**
1. Запустите PowerShell от имени администратора
2. Повторите установку

### winget не работает

**Решение:**
1. Обновите Windows
2. Установите App Installer из Microsoft Store
3. Или используйте Chocolatey/ручную установку

---

## Альтернатива: Использовать текстовый режим

Если не хотите устанавливать ffmpeg:

1. Используйте текстовый режим: `/mode text`
2. Отправляйте текстовые сообщения вместо голосовых
3. Все остальные функции будут работать

---

## Проверка в боте

После установки ffmpeg отправьте голосовое сообщение боту.

**Должно быть:**
```
🎤 Распознано:
[ваш текст]

[Ответ бота]
```

**Не должно быть:**
```
❌ Ошибка обработки голосового сообщения
Требуется установка ffmpeg
```

---

## Дополнительная информация

### Что делает ffmpeg в боте?

1. **Конвертация аудио:**
   - Telegram отправляет голосовые в формате OGG
   - OpenAI Whisper требует MP3/WAV
   - ffmpeg конвертирует OGG → MP3

2. **Обработка:**
   ```python
   # В services/stt.py
   audio = AudioSegment.from_file(voice_path)
   audio.export(mp3_path, format="mp3")
   ```

### Размер установки

- **winget/choco:** ~100 MB
- **Вручную (essentials):** ~80 MB
- **Вручную (full):** ~200 MB

Рекомендуется essentials версия.

---

## Итог

1. ✅ Установите ffmpeg через winget
2. ✅ Перезапустите терминал
3. ✅ Проверьте: `ffmpeg -version`
4. ✅ Перезапустите бота
5. ✅ Отправьте голосовое сообщение

Готово! 🎉
